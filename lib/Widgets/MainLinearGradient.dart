import 'package:flutter/material.dart';

class MainLinearGradient {
  static LinearGradient mainlinearGradient = LinearGradient(
      colors: [Colors.blue[300]!, Colors.white],
      begin: const Alignment(0, -1),
      end: const Alignment(0, 1));
}
